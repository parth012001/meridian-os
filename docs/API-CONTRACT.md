# API contract between the backend session and the frontend session

The backend session (branch `feat/earned-autonomy`) appends here. The frontend session (worktree `../meridian-web`, branch `feat/web`) builds against it. Existing endpoints are in `src/server.ts`; only new or changed ones are listed below.

_(nothing yet)_
